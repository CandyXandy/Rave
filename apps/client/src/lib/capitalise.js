/**
 * Capitalise a string.
 *
 * @param {string} string The string to capitalise.
 * @returns {string} A capitalised string.
 */
const capitalise = (string) => string.charAt(0).toUpperCase() + string.slice(1);

export default capitalise;
